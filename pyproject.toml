[project]
name = "long-sus"
version = "0.1.0"
description = "Longevity-intervention population query toolkit"
requires-python = ">=3.11"
dependencies = [
  "numpy",
  "pandas",
  "scipy",
  "matplotlib",
  "fastapi",
  "uvicorn",
]

[project.scripts]
long-sus = "long_sus.cli:main"
long-sus-api = "long_sus.api:main"

[tool.pytest.ini_options]
pythonpath = [
  "src",
]
testpaths = [
  "tests",
]
